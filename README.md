# Master
scripte verwendet fuer meine masterarbeit


zu grosse dateien liegen auf dem server (eine jupyter fuer bestimmte tests) und die datasets.
auf dem Server in meinem Bereich unter directory master.
